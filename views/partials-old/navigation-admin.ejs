<nav class="navbar navbar-expand-lg sticky-top navbar-light ">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href=""
      ><img src="/images/logo.png" height="30" width="30" /> E-Care</a
    >

    <style>
      .navbar{
        background-color:#45637d;
        
      }
    </style>

    <button
      class="navbar-toggler"
      type="button"
      data-mdb-toggle="collapse"
      data-mdb-target="#navbarText"
      aria-controls="navbarText"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <i class="fas fa-bars"></i>
    </button>
    <div class="collapse navbar-collapse p-2" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0"></ul>
      <%if(typeof (locals.username) == "undefined"){ %>
      <a
        class="nav-link fw-bold text-decoration-none text-primary"
        href="/login"
        >Login</a
      >
      <a
        class="nav-link fw-bold text-decoration-none text-danger"
        href="/register"
        >Register</a
      >
      <%}else{%>

      <a
        class="nav-link fw-bold text-decoration-none text-warning"
        href="/logout"
        >Logout </a>

      <% } %>
    </div>
  </div>
</nav>
